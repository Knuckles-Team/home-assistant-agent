{
  "mcpServers": {
    "home": {
      "command": "home-assistant-mcp",
      "args": [
        "--transport",
        "stdio"
      ],
      "env": {
        "HOME_URL": "${HOME_URL:-http://localhost:8123}",
        "HOME_TOKEN": "${HOME_TOKEN}"
      }
    }
  }
}
